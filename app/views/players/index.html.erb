<%# <h1>Welcome <%= current_user.first_name %></h1> %>

<h1>Players list</h1>
<ul>
  <% @players.each do |player| %>
    <li><%= link_to player.name, player_path(player) %></li>
    <li><%= image_tag(player.photo) %></li>
  <% end %>
</ul>


<p>
  <%= link_to "Add a new player", new_player_path %>
</p>
